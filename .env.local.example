# ChatVRM Frontend env (optional)
# Set this to your deployed Python backend URL for production/mobile builds
# Example: NEXT_PUBLIC_API_URL=https://your-backend.onrender.com
# When empty, the app uses the Next.js API proxy (/api) in dev mode
NEXT_PUBLIC_API_URL=
